sed -i.bak 's/const { width, height } = event.naturalSize;/let { width, height, orientation } = event.naturalSize;\n      if (orientation === "portrait" \&\& width > height) {\n        const temp = width;\n        width = height;\n        height = temp;\n      } else if (orientation === "landscape" \&\& height > width) {\n        const temp = width;\n        width = height;\n        height = temp;\n      }/g' src/components/Report/KineticVideoPlayer.native.tsx
